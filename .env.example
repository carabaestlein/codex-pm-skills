# Copy this to your automation environment, not necessarily into the repo.

# Email delivery
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USERNAME=your-smtp-user
SMTP_PASSWORD=your-smtp-password
MARKET_INTEL_FROM=pm-intel@example.com
MARKET_INTEL_TO=you@example.com

# Optional model/API settings for automation implementations
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4.1-mini
